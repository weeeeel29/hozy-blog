{
  "name": "hozy-blog",
  "version": "1.0.0",
  "private": true,
  "description": "整理手記 —— 寫「整理系」遊戲的中文部落格，目前收錄《Hozy》與《Librarian》（靜態網站，零執行期相依）",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && wrangler pages dev dist --port 8788",
    "deploy": "node build.mjs && wrangler pages deploy --project-name hozy-blog --branch main --commit-dirty=true",
    "new": "node new-post.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
